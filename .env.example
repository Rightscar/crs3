# Environment Variables for Universal Document Reader
# =================================================

# OPENAI INTEGRATION (Optional)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# STREAMLIT CONFIGURATION (Auto-set by Render)
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
STREAMLIT_SERVER_ENABLE_CORS=false
STREAMLIT_SERVER_ENABLE_XSRF_PROTECTION=false

# SERVER CONFIGURATION (Auto-set by Render)
PORT=10000

# APPLICATION SETTINGS (Optional)
MAX_FILE_SIZE_MB=200
DEFAULT_THEME=light
LOG_LEVEL=INFO

# DEPLOYMENT ENVIRONMENT
DEPLOYMENT_PLATFORM=render
ENVIRONMENT=production

# INSTRUCTIONS:
# 1. Copy this file to .env in your project root
# 2. Replace 'your_openai_api_key_here' with your actual OpenAI API key
# 3. Most other variables are automatically set by Render
# 4. Only add OPENAI_API_KEY if you want AI features
# 5. The app works perfectly without OpenAI (demo mode)