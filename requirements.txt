# Universal Text-to-Dialogue AI - Render Deployment
# Minimal dependencies for reliable deployment

# Core Framework
streamlit==1.28.1

# Essential Dependencies
pandas==2.0.3
numpy==1.24.3

# Document Processing (Optional - graceful fallback if missing)
PyPDF2==3.0.1

# AI Integration (Optional - demo mode if missing)
openai==1.3.8

# Basic Utilities
requests==2.31.0
python-dotenv==1.0.0

